using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testscript : MonoBehaviour
{
/*
    int viePika = 30
    int vieBulbi = 40

    // Start is called before the first frame update
    void Start()
    {
        bool isVivant = true;
        string pokemon; 
        pokemon = "pikachu";
        print(pokemon);
        pokemon = "bulbizarre"; 

        print(isVivant);
        print(pokemon);
        print(isVivant + " " + pokemon);
        print("le pokemon s'appelle " + pokemon);
    }

    // Update is called once per frame
    void Update()
    {
    
        if (Input.GetKeyDown(KeyCode.P))
        {
            int degat = 5;
            print("Pikachu lance éclair");
            vieBulbi = vieBulbi - degat;
            print("il reste " + vieBulbi + " points de vie a Bulbizarre");
        }
        
        if (Input.GetKeyDown(KeyCode.B))
        {
            int degat = 3;
            print("Bulbizarre lance engrais");
            viePika = viePika - degat;
            print("il reste " + viePika + " points de vie à Pikachu");
        }
        
        
    }*/
}
